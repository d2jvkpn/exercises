fn main() {
    let mut vec = [5, 10, 1, 4, 11];
    let expected = [1, 4, 5, 10, 11];
}

fn merge<T: PartialOrd>(vec: &mut [T]) {
    let len = vec.len();
    if len < 2 {
        return;
    }

	// TODO
    return;
}
